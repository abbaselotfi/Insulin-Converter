# Diabetes Clinical Decision Support Platform

پلتفرم دوزبانهٔ فارسی و انگلیسی برای پشتیبانی تصمیم‌گیری بالینی پزشکان در مدیریت دیابت.

> **وضعیت پروژه:** نسخهٔ آزمایشی رابط دوزبانه و ماشین‌حساب تبدیل انسولین قابل اجرا است؛ قواعد بالینی هنوز باید پیش از استفادهٔ واقعی به‌صورت قاعده‌به‌قاعده بازبینی و تأیید شوند. این سامانه جایگزین قضاوت بالینی پزشک نیست.

## چشم‌انداز / Vision

این سامانه قرار است راهنماهای معتبر ADA و EASD را به پیشنهادهای قابل‌ردیابی و نسخه‌بندی‌شده تبدیل کند؛ بدون آن‌که نقش پزشک را در تصمیم نهایی جایگزین کند. رابط کاربری و محتوای بالینی به فارسی و انگلیسی ارائه می‌شوند و فهرست داروها می‌تواند نام‌های ژنریک و برندهای موجود در بازار ایران را با اولویت قابل‌تنظیم نمایش دهد.

The platform turns authoritative ADA and EASD guidance into traceable, versioned recommendations while keeping the clinician responsible for the final decision. Clinical content and user interfaces are available in Persian and English, with configurable generic/brand medication display for the Iranian market.

## وضعیت فعلی / Current scope

- رابط بالینی واکنش‌گرا با پشتیبانی فارسی/انگلیسی و RTL/LTR؛
- مسیرهای تبدیل انسولین پایه، سریع‌الاثر/رگولار، مخلوط و FRC؛
- انتخاب خودکار قلم هلویی یا زیتونی Suliqua بر اساس قاعدهٔ مستندشده؛
- موتور محاسبات مستقل و آزمون‌های رگرسیون؛
- Manifest و Service Worker برای اجرای PWA؛
- ثبت منبع، تاریخ بازبینی و مرز تأیید قواعد بالینی.

## اجرای محلی / Run locally

Node.js 22 یا جدیدتر لازم است. در ریشهٔ مخزن اجرا کنید:

```bash
npm install
npm run dev
```

آدرس محلی اعلام‌شده توسط Vite (معمولاً `http://localhost:5173`) را باز کنید. آزمون و build تولید:

```bash
npm test
npm run build
```

## قابلیت‌های هدف / Planned capabilities

- ثبت داده‌های ضروری بیمار و تولید پیشنهاد همراه با دلیل، منبع و نسخهٔ قانون؛
- نسخه‌بندی مستقل مجموعه‌قوانین بالینی و انتشار کنترل‌شدهٔ آن‌ها؛
- به‌روزرسانی سالانه و به‌روزرسانی اضطراری راهنماهای ADA و EASD؛
- مدیریت داروهای ژنریک، شکل و دوز دارویی، برند ایرانی، تولیدکننده و وضعیت بازار؛
- انتخاب اولویت نمایش `generic-first` یا `brand-first` در سطح سازمان و کاربر؛
- پنل مدیریت دوزبانه برای کاربران غیر برنامه‌نویس با گردش‌کار پیش‌نویس، بازبینی، تأیید و انتشار؛
- داشبورد مدیریتی برای مشاهدهٔ تعداد کاربران و میزان استفاده از سامانه با رعایت حریم خصوصی؛
- ثبت رویدادهای ممیزی و امکان بازسازی خروجی تاریخی سامانه.

## ساختار اجرایی فعلی

```text
src/
  App.tsx                 # رابط React و جریان ماشین‌حساب
  clinical/engine.js      # موتور مستقل محاسبات و قواعد ایمنی
  clinical/engine.test.js # آزمون‌های Vitest
  i18n/translations.ts    # ترجمه‌های فارسی و انگلیسی
  main.tsx                # نقطهٔ ورود برنامه
  styles.css              # طراحی واکنش‌گرا و RTL/LTR
public/                   # Manifest، Service Worker، آیکن‌ها و صفحات ثابت
.github/workflows/        # آزمون، build و استقرار GitHub Pages
```

`index.html` فقط میزبان استاندارد Vite است؛ رابط برنامه به شکل مؤلفه‌ای در React نگهداری می‌شود و
خروجی production از طریق `npm run build` در `dist/` ساخته می‌شود.

## اصول ایمنی

1. هر پیشنهاد باید نسخهٔ قانون، نسخهٔ راهنما، زمان ارزیابی و داده‌های ورودی مؤثر را ثبت کند.
2. محتوای پیش‌نویس یا منقضی هرگز در موتور فعال اجرا نمی‌شود.
3. تغییر بالینی با تأیید حداقل یک بازبین بالینی مجاز منتشر می‌شود؛ نویسنده نباید تنها تأییدکننده باشد.
4. در نبود دادهٔ کافی، سامانه به‌جای حدس‌زدن، محدودیت و نیاز به بررسی پزشک را اعلام می‌کند.
5. توصیه‌ها توضیح‌پذیرند و پیوند دقیق به منبع دارند؛ سامانه ابزار پشتیبان است، نه ابزار تشخیص یا تجویز خودکار.

## نقشهٔ اسناد / Documentation map

| سند | موضوع |
| --- | --- |
| [معماری](docs/ARCHITECTURE.md) | اجزا، جریان درخواست، امنیت، بومی‌سازی و استقرار |
| [پنل مدیریت](docs/ADMIN_PANEL.md) | نقش‌ها، گردش‌کار انتشار و تجربهٔ کاربر غیر برنامه‌نویس |
| [مدل داده](docs/DATA_MODEL.md) | موجودیت‌ها، ارتباط‌ها، تاریخچه و قواعد تمامیت |
| [راهبرد به‌روزرسانی راهنما](docs/GUIDELINE_UPDATE_STRATEGY.md) | دریافت، تطبیق، آزمون، انتشار و بازگشت نسخه‌های ADA/EASD |
| [ثبت منابع بالینی](docs/CLINICAL_SOURCES.md) | منابع پیاده‌سازی‌شده، تاریخ بازبینی و مرز تأیید قواعد |

## ساختار بلندمدت پیشنهادی مخزن

```text
apps/
  web/                 # رابط پزشک و پنل مدیریت دوزبانه
  api/                 # API کاربردی و مدیریت
packages/
  clinical-engine/     # موتور قطعی ارزیابی قوانین
  shared/              # قراردادها و ابزارهای مشترک
content/               # محتوای نسخه‌بندی‌پذیر و seedهای تأییدشده
docs/                  # تصمیم‌های معماری و محصول
```

انتخاب فناوری و ایجاد این پوشه‌ها به یک تصمیم معماری بعدی موکول شده است؛ ساختار بالا قرارداد مفهومی است، نه وضعیت فعلی مخزن.

## شروع مشارکت / Contributing

در این مرحله تغییرات باید ابتدا اسناد را به‌روز کنند. هر تغییر مرتبط با محتوای بالینی باید منبع، تاریخ دسترسی، راهنمای مبنا، اثر مورد انتظار و سناریوهای آزمون را مشخص کند. اطلاعات واقعی بیمار، شناسه‌های مستقیم و اسناد دارای مجوز نامشخص نباید در مخزن ثبت شوند.

## سلب مسئولیت / Disclaimer

این پروژه در حال توسعه است و برای استفادهٔ بالینی آماده نیست. خروجی آیندهٔ آن نیز باید فقط با بررسی متخصص واجد صلاحیت استفاده شود.
